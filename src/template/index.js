function parseInput(input) {
    return input;
}

function run(input, part) {
    input = parseInput(input)

    if(part === 1){

    }

    if(part === 2) {
        
    }
}

module.exports = { 
    run,
    samples: []
};